import * as React from 'react';
import AppRouter from '../../../AppRouter';

class Main extends React.Component {
  render() {
    return (
      <div>
        <AppRouter />
      </div>
    );
  }
}

export default Main;
