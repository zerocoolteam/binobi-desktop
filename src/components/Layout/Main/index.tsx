import * as React from 'react';
import Router from '../../../Router';

class Main extends React.Component {
    render() {
        return (
            <div>
                <Router />
            </div>
        );
    }
}

export default Main;
